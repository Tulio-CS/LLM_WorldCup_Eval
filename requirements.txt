# Core (always required)
pandas>=2.0
openpyxl>=3.1
python-dotenv>=1.0
requests>=2.31          # used by the `fetch` command (football-data.org)

# Web dashboard (served as the Coolify web service; see fifa_forecast/web.py)
fastapi>=0.111
uvicorn[standard]>=0.29
python-multipart>=0.0.9   # FastAPI form handling for the dashboard buttons
sse-starlette>=1.8        # reserved for live job-log streaming

# Provider SDKs (optional — install the ones you need).
# Without these, the corresponding provider can only run in --dry-run (mock) mode.
openai>=1.40          # OpenAI GPT-5 family AND xAI Grok (OpenAI-compatible API)
anthropic>=0.40       # Claude Opus / Sonnet
google-genai>=0.3     # Gemini 2.5 Pro / Flash
